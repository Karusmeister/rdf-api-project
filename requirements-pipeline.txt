# Pipeline runner dependencies (Cloud Run Job)
# Kept separate from requirements.txt to keep the API image lean.

# Core (shared with API)
fastapi>=0.110
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
psycopg2-binary>=2.9

# Parsing / numerics
lxml>=5.1
python-dateutil>=2.9

# Pipeline-specific
pyarrow>=15.0
pandas>=2.1
joblib>=1.3

# GCP
google-cloud-bigquery>=3.20
google-cloud-storage>=2.14

# ML (optional — Maczynska is fully analytical; these enable XGBoost/sklearn models)
scikit-learn>=1.4
xgboost>=2.0
